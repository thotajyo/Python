#The strip() method in Python is used to remove leading (at the beginning) and trailing (at the end) characters from a string. 
#By default, it removes whitespace, but you can specify other characters to remove as well.
text  = "     some spaces around     "
stripped_text = text.strip() #text.strip() - removes those spaces from text.
print("stripped text:", stripped_text)